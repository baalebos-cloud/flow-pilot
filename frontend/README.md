# FlowPilot frontend

This directory is reserved for the Flutter application.

Before generating the project, the frontend owner should confirm:

- Flutter package/application identifier.
- Android and iOS targets.
- State-management approach.
- Environment/configuration strategy.
- How the BMONI embedded SDK packages are obtained.

Shared API and trust-boundary documentation lives in the repository-level [`docs`](../docs/) directory.

